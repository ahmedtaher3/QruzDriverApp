mutation StartSeatTrip ($trip_id: ID! ,$trip_time: String! ,$latitude: String!,$longitude: String! ){

    startSeatsTrip(input:{
        trip_id: $trip_id
        trip_time: $trip_time
        latitude: $latitude
        longitude: $longitude
    }) {
        log_id
        id
        name
    }

}
