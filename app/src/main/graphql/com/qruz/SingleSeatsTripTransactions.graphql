query SingleSeatsTripTransactions ($trip_id: ID!, $trip_time: String! ){

    singleSeatsTripTransactions(trip_id: $trip_id, trip_time:$trip_time) {
        paid
        created_by
        user {
            id
            name
            avatar
         }
    }

}