mutation EndTrip ( $trip_id: ID! ,$log_id: String! ,$latitude: String! ,$longitude: String! ){

    endTrip(input:{
        trip_id: $trip_id
        log_id: $log_id
        latitude: $latitude
        longitude: $longitude
    })


}